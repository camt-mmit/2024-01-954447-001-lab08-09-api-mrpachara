import { SwNumberDirective } from './sw-number.directive';

describe('SwNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SwNumberDirective();
    expect(directive).toBeTruthy();
  });
});
